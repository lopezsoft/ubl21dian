import { BaseXmlSigner } from './BaseXmlSigner';

/**
 * Firmante estándar para documentos UBL (Facturas, Notas, etc.).
 * Extiende la clase base sin necesidad de modificar el algoritmo de firma.
 */
export class XmlSigner extends BaseXmlSigner {}